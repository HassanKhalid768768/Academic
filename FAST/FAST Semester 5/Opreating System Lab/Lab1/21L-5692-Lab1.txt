-- Question 1

# Terminal commands
1. pwd
2. ls
3. cd
4. cp
5. mkdir new_directory
6. rmdir empty_directory
7. man ls
8. sudo command_name
9. apt-get install package_name

-- Question 2

#include <stdio.h>

int main(int argc, char **argv) {
    printf("Welcome to BSBS Operating System Lab Course\n");
    printf("Course Name: %s\n", argv[1]);
    return 0;
}

-- Question 3

#include <stdio.h>
#include <stdlib.h>

int main(int argc, char **argv) {
    int sum = 0;
    float average = 0;
    int i = 0;

    for (i = 1; i < argc; i++) {
        int number = atoi(argv[i]);
        sum += number;
    }

    average = (float)sum / (argc - 1);

    printf("Sum of numbers: %d\n", sum);
    printf("Average of numbers: %f\n", average);

    return 0;
}

-- Question 4

#include <stdio.h>
#include <ctype.h>

int main(int argc, char **argv) {
    if (argc != 3) {
        printf("Usage: %s input_file output_file\n", argv[0]);
        return 1;
    }

    FILE *input_file = fopen(argv[1], "r");
    if (input_file == NULL) {
        printf("Unable to open input file.\n");
        return 1;
    }

    FILE *output_file = fopen(argv[2], "w");
    if (output_file == NULL) {
        printf("Unable to open output file.\n");
        fclose(input_file);
        return 1;
    }

    int character;
    while ((character = fgetc(input_file)) != EOF) {
        if (isdigit(character)) {
            fputc(character, output_file);
        }
    }

    fclose(input_file);
    fclose(output_file);

    return 0;
}